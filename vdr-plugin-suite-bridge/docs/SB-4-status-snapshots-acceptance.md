# SB.4 Acceptance

The implementation is complete when all source contracts, standalone C++ tests,
the VDR-header plugin build, API-suffixed staging installation, controlled live
snapshot logging and full rollback have passed.
