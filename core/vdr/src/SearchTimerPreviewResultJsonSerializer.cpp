#include "SearchTimerPreviewResultJsonSerializer.h"

// Intentionally empty.
//
// SearchTimerPreviewResultJsonSerializer is implemented inline in the header.
// Keeping this translation unit preserves existing Makefile source lists while
// avoiding duplicate symbol definitions in targets that already pull the
// serializer through $(VDR_SRC).
