// Channels 2 platform module: responsive channel/day programme with safe actions.
(function (global) {
  'use strict';

  const CSS = `
.channels2{display:grid;gap:1rem;min-width:0}.channels2 h3,.channels2 p{margin:0}.channels2-toolbar{display:flex;align-items:flex-end;justify-content:space-between;gap:1rem}.channels2-toolbar>div:first-child{display:grid;gap:.2rem}.channels2-toolbar>div:first-child p{color:#94a3b8}.channels2-tools{display:flex;flex-wrap:wrap;gap:.5rem}.channels2-search{width:min(25rem,100%);min-height:2.7rem;padding:.65rem .8rem;border:1px solid #475569;border-radius:.72rem;background:#0f172a;color:#f8fafc;font:inherit}.channels2-status{padding:.7rem .85rem;border:1px solid rgba(148,163,184,.24);border-radius:.75rem;background:rgba(15,23,42,.72);color:#cbd5e1}.channels2-status.error,.channels2-feedback.error{border-color:rgba(248,113,113,.45);color:#fecaca}.channels2-feedback.success{color:#bbf7d0}.channels2-grid{display:grid;grid-template-columns:minmax(15rem,21rem) minmax(0,1fr);gap:1rem;align-items:start}.channels2-list{display:grid;gap:.4rem;max-height:72vh;overflow:auto;padding-right:.2rem}.channels2-channel{display:grid;grid-template-columns:3.5rem minmax(0,1fr);gap:.65rem;align-items:center;width:100%;padding:.62rem;border:1px solid rgba(148,163,184,.18);border-radius:.78rem;background:rgba(15,23,42,.68);color:#f8fafc;text-align:left}.channels2-channel:hover,.channels2-channel:focus-visible,.channels2-channel.active{border-color:rgba(56,189,248,.62);background:rgba(14,165,233,.14);outline:none}.channels2-logo{display:grid;place-items:center;width:3.5rem;height:2.4rem}.channels2-logo img,.channels2-logo .channel-logo{max-width:100%;max-height:100%;object-fit:contain}.channels2-title{display:block;font-weight:800;overflow-wrap:anywhere}.channels2-meta{display:block;margin-top:.12rem;color:#94a3b8;font-size:.82rem}.channels2-program{display:grid;gap:.7rem;min-width:0}.channels2-head{display:flex;flex-wrap:wrap;align-items:center;justify-content:space-between;gap:.65rem;padding:.75rem .82rem;border:1px solid rgba(56,189,248,.25);border-radius:.82rem;background:rgba(2,6,23,.64)}.channels2-date{display:flex;flex-wrap:wrap;gap:.4rem}.channels2-date button,.channels2-date input,.channels2-tools button{min-height:2.55rem;padding:.52rem .7rem;border-radius:.68rem}.channels2-date input{border:1px solid #475569;background:#0f172a;color:#f8fafc;font:inherit}.channels2-day-heading{padding:.15rem .1rem;color:#e2e8f0;font-size:1rem}.channels2-events{display:grid;gap:.42rem}.channels2-event{display:grid;grid-template-columns:6rem minmax(0,1fr) auto;gap:.65rem;align-items:center;width:100%;padding:.68rem .75rem;border:1px solid rgba(148,163,184,.18);border-radius:.78rem;background:rgba(15,23,42,.68);color:#f8fafc;text-align:left}.channels2-event:hover,.channels2-event:focus-visible,.channels2-event.active{border-color:rgba(56,189,248,.62);background:rgba(14,165,233,.12);outline:none}.channels2-event.current{border-color:rgba(74,222,128,.5)}.channels2-time{font-weight:800;color:#bae6fd}.channels2-badge{padding:.18rem .4rem;border:1px solid rgba(148,163,184,.28);border-radius:999px;color:#cbd5e1;font-size:.7rem;font-weight:800}.channels2-detail{display:grid;gap:.8rem;padding:1rem;border:1px solid rgba(56,189,248,.32);border-radius:.9rem;background:rgba(2,6,23,.78)}.channels2-detail-header{display:grid;gap:.25rem}.channels2-detail-subtitle{color:#e2e8f0;font-size:1rem}.channels2-detail-time{color:#93c5fd;font-weight:700}.channels2-description{max-width:75ch;color:#dbe4f0;line-height:1.5;white-space:pre-line}.channels2-actions{display:flex;flex-wrap:wrap;gap:.5rem;padding-top:.1rem}.channels2-actions button{min-height:2.7rem;padding:.58rem .9rem;border-radius:.7rem}.channels2-primary-action{font-weight:800}.channels2-secondary-action{background:transparent!important;border:1px solid rgba(96,165,250,.6)!important;color:#bfdbfe!important}.channels2-feedback{min-height:1.35rem;padding:.45rem 0;color:#cbd5e1}.channels2-empty{padding:1rem;border:1px dashed rgba(148,163,184,.3);border-radius:.8rem;color:#94a3b8;text-align:center}
@media(max-width:900px){.channels2-grid{grid-template-columns:1fr}.channels2-list{max-height:35vh}.channels2-toolbar{align-items:stretch;flex-direction:column}.channels2-tools{display:grid;grid-template-columns:minmax(0,1fr) auto}.channels2-search{width:100%}}
@media(max-width:600px){.channels2{gap:.75rem}.channels2-status{font-size:.85rem}.channels2-list{display:flex;max-height:none;overflow-x:auto;overflow-y:hidden;padding:.1rem 0 .45rem;scroll-snap-type:x proximity}.channels2-channel{flex:0 0 13.5rem;scroll-snap-align:start}.channels2-head{align-items:stretch;flex-direction:column}.channels2-date{display:grid;grid-template-columns:auto 1fr auto}.channels2-date input{grid-column:1/-1;width:100%}.channels2-event{grid-template-columns:4.8rem minmax(0,1fr);align-items:start}.channels2-event .channels2-badge{display:none}.channels2-detail{margin-inline:-.15rem;padding:.85rem;border-radius:.8rem}.channels2-description{max-height:16rem;overflow:auto;padding-right:.25rem;font-size:.96rem;line-height:1.42}.channels2-actions{position:sticky;bottom:0;z-index:3;display:grid;grid-template-columns:1fr 1fr;margin:.2rem -.85rem -.85rem;padding:.7rem .85rem;background:linear-gradient(to top,#020617 75%,rgba(2,6,23,.88))}.channels2-actions button{width:100%;padding:.58rem .5rem;font-size:.9rem}.channels2-feedback{font-size:.9rem}.channels2-tools{grid-template-columns:1fr}.channels2-tools button{width:100%}}
`;

  const state = {active:false,channels:[],visible:[],channel:null,day:new Date(),events:[],event:null,filter:'',sequence:0};
  const platform = () => global.VdrSuitePlatform || null;
  const clientApi = () => platform() && platform().getClientApi ? platform().getClientApi() : global.VdrSuiteClientApi;
  const mount = () => platform() && platform().getMountTarget ? (platform().getMountTarget('channels2') || platform().getMountTarget('detail')) : document.getElementById('detail-data');
  const backendId = () => String(platform() && platform().getSelectedBackendId ? platform().getSelectedBackendId() : 'default') || 'default';
  const pick = (o, keys, fallback='') => { for (const k of keys) if (o && o[k] !== undefined && o[k] !== null && o[k] !== '') return o[k]; return fallback; };
  const text = v => String(v == null ? '' : v).trim();
  const list = (data,key) => Array.isArray(data) ? data : data && Array.isArray(data[key]) ? data[key] : data && Array.isArray(data.items) ? data.items : data && Array.isArray(data.results) ? data.results : [];
  const epoch = v => { const n=Number(v); if(Number.isFinite(n)&&n>0)return n>1e11?Math.floor(n/1000):Math.floor(n); const p=Date.parse(String(v||'')); return Number.isFinite(p)?Math.floor(p/1000):0; };
  const channelId = c => text(pick(c,['id','channelId','nativeId']));
  const channelName = c => text(pick(c,['name','channelName','title','displayName'],'Kanal'));
  const channelNumber = c => Number(pick(c,['number','channelNumber','position'],0))||0;
  const channelGroup = c => text(pick(c,['group','groupName','channelGroup','bouquet','provider']));
  const eventTitle = e => text(pick(e,['title','name','eventTitle'],'Sendung'));
  const eventSubtitle = e => text(pick(e,['subtitle','shortText','short_text']));
  const eventDescription = e => text(pick(e,['description','longText','long_text','summary']));
  const eventChannelId = e => text(pick(e,['channelId','channel','channel_id']));
  const eventStart = e => epoch(pick(e,['startTime','start','beginTime'],0));
  const eventEnd = e => { const s=eventStart(e),x=epoch(pick(e,['endTime','end','stopTime'],0)),d=Number(pick(e,['durationSeconds','duration'],0)); return x>s?x:s+(Number.isFinite(d)&&d>0?d:0); };
  const addText = (el,v) => { el.textContent=String(v); return el; };
  const dayOnly = v => { const d=v instanceof Date?v:new Date(v||Date.now()); return new Date(d.getFullYear(),d.getMonth(),d.getDate()); };
  const moveDay = (v,n) => { const d=dayOnly(v); d.setDate(d.getDate()+n); return d; };
  const dateValue = v => { const d=dayOnly(v); return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; };
  const clock = v => new Date(Number(v)*1000).toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'});
  const hhmm = v => { const d=new Date(Number(v)*1000); return d.getHours()*100+d.getMinutes(); };

  function ensureStyles(){if(document.getElementById('vdr-suite-channels2-styles'))return;const style=document.createElement('style');style.id='vdr-suite-channels2-styles';style.textContent=CSS;document.head.appendChild(style);}
  function timerPayload(event,channel){const start=eventStart(event),end=eventEnd(event),eventId=text(pick(event,['eventId','id','nativeId']));return{backendId:backendId(),channelId:eventChannelId(event)||channelId(channel),title:eventTitle(event),directory:'',day:dateValue(new Date(start*1000)),weekdays:'-------',start:hhmm(start),stop:hhmm(end),priority:50,lifetime:99,active:true,vps:false,aux:eventId?'eventId='+eventId:''};}
  function createTimer(event,channel,feedback,button){const api=clientApi();if(!api||typeof api.fetchClientTimerCreateAction!=='function'){feedback.className='channels2-feedback error';feedback.textContent='Timer-API ist nicht verfügbar.';return;}button.disabled=true;feedback.className='channels2-feedback';feedback.textContent='Timer wird erstellt …';api.fetchClientTimerCreateAction({payload:timerPayload(event,channel),cache:'no-store',credentials:'same-origin'}).then(result=>{if(result&&result.success===false)throw new Error(result.message||result.error||'Aktion wurde abgelehnt.');button.textContent='Timer erstellt';feedback.className='channels2-feedback success';feedback.textContent='Timer für „'+eventTitle(event)+'“ wurde erstellt.';}).catch(error=>{button.disabled=false;feedback.className='channels2-feedback error';feedback.textContent='Timer konnte nicht erstellt werden: '+error.message;});}
  function ensureEditorApi(){if(global.VdrSuiteEpgSearchTimerActions&&typeof global.VdrSuiteEpgSearchTimerActions.openSearchTimerEditor==='function')return Promise.resolve(global.VdrSuiteEpgSearchTimerActions);return Promise.reject(new Error('SearchTimer-Editor ist nicht verfügbar.'));}
  function prepareSearchTimer(event,channel,detail){const feedback=detail.querySelector('.channels2-feedback');feedback.className='channels2-feedback';feedback.textContent='SearchTimer-Editor wird geöffnet …';ensureEditorApi().then(editor=>editor.openSearchTimerEditor({title:eventTitle(event),channelId:eventChannelId(event)||channelId(channel),channelGroup:channelGroup(channel),statusTarget:detail})).catch(error=>{feedback.className='channels2-feedback error';feedback.textContent=error.message;});}
  function filterChannels(){const q=state.filter.toLocaleLowerCase('de-DE');state.visible=state.channels.filter(c=>!q||channelName(c).toLocaleLowerCase('de-DE').includes(q)||channelGroup(c).toLocaleLowerCase('de-DE').includes(q)||String(channelNumber(c)).includes(q));}
  function renderDetail(parent){if(!state.event||!state.channel)return;const detail=document.createElement('article');detail.className='channels2-detail';const header=document.createElement('header');header.className='channels2-detail-header';header.appendChild(addText(document.createElement('h3'),eventTitle(state.event)));if(eventSubtitle(state.event)){const p=addText(document.createElement('p'),eventSubtitle(state.event));p.className='channels2-detail-subtitle';header.appendChild(p);}const meta=addText(document.createElement('p'),clock(eventStart(state.event))+'–'+clock(eventEnd(state.event))+' · '+channelName(state.channel));meta.className='channels2-detail-time';header.appendChild(meta);detail.appendChild(header);const description=addText(document.createElement('p'),eventDescription(state.event)||'Keine Beschreibung vorhanden.');description.className='channels2-description';detail.appendChild(description);const actions=document.createElement('div');actions.className='channels2-actions';const timer=addText(document.createElement('button'),'Timer erstellen');timer.type='button';timer.className='channels2-primary-action';const series=addText(document.createElement('button'),'Serientimer vorbereiten');series.type='button';series.className='channels2-secondary-action';const feedback=document.createElement('p');feedback.className='channels2-feedback';feedback.setAttribute('role','status');feedback.setAttribute('aria-live','polite');timer.onclick=()=>createTimer(state.event,state.channel,feedback,timer);series.onclick=()=>prepareSearchTimer(state.event,state.channel,detail);actions.append(timer,series);detail.append(actions,feedback);parent.appendChild(detail);}
  function render(){ensureStyles();const target=mount();if(!target)return;target.replaceChildren();const root=document.createElement('section');root.className='channels2';const toolbar=document.createElement('header');toolbar.className='channels2-toolbar';const intro=document.createElement('div');intro.append(addText(document.createElement('h3'),'Channels 2'),addText(document.createElement('p'),'Kanal auswählen, Tagesprogramm prüfen und Timer sicher vorbereiten.'));const tools=document.createElement('div');tools.className='channels2-tools';const search=document.createElement('input');search.type='search';search.className='channels2-search';search.placeholder='Kanal, Nummer oder Gruppe suchen';search.value=state.filter;const reload=addText(document.createElement('button'),'Neu laden');reload.type='button';tools.append(search,reload);toolbar.append(intro,tools);root.appendChild(toolbar);const status=addText(document.createElement('p'),state.channels.length+' Kanäle geladen.');status.className='channels2-status';root.appendChild(status);const grid=document.createElement('div');grid.className='channels2-grid';const channelList=document.createElement('section');channelList.className='channels2-list';const program=document.createElement('section');program.className='channels2-program';grid.append(channelList,program);root.appendChild(grid);target.appendChild(root);const renderChannels=()=>{channelList.replaceChildren();state.visible.forEach(channel=>{const button=document.createElement('button');button.type='button';button.className='channels2-channel';if(state.channel&&channelId(state.channel)===channelId(channel))button.classList.add('active');const logo=document.createElement('span');logo.className='channels2-logo';if(typeof global.createChannelLogoElement==='function')logo.appendChild(global.createChannelLogoElement(channelName(channel),channelId(channel)));else logo.textContent=String(channelNumber(channel)||'•');const copy=document.createElement('span');const title=addText(document.createElement('span'),channelName(channel));title.className='channels2-title';const meta=addText(document.createElement('span'),'Nr. '+(channelNumber(channel)||'-')+(channelGroup(channel)?' · '+channelGroup(channel):''));meta.className='channels2-meta';copy.append(title,meta);button.append(logo,copy);button.onclick=()=>{state.channel=channel;state.event=null;render();loadEvents();};channelList.appendChild(button);});};const renderProgram=()=>{program.replaceChildren();if(!state.channel){const empty=addText(document.createElement('div'),'Einen Kanal auswählen.');empty.className='channels2-empty';program.appendChild(empty);return;}const head=document.createElement('article');head.className='channels2-head';const copy=document.createElement('div');copy.append(addText(document.createElement('h3'),channelName(state.channel)),addText(document.createElement('p'),'Kanal '+(channelNumber(state.channel)||'-')+(channelGroup(state.channel)?' · '+channelGroup(state.channel):'')));const controls=document.createElement('div');controls.className='channels2-date';const prev=addText(document.createElement('button'),'←'),today=addText(document.createElement('button'),'Heute'),next=addText(document.createElement('button'),'→');prev.type=today.type=next.type='button';const input=document.createElement('input');input.type='date';input.value=dateValue(state.day);prev.onclick=()=>{state.day=moveDay(state.day,-1);loadEvents();};today.onclick=()=>{state.day=dayOnly(new Date());loadEvents();};next.onclick=()=>{state.day=moveDay(state.day,1);loadEvents();};input.onchange=()=>{state.day=dayOnly(input.value);loadEvents();};controls.append(prev,today,next,input);head.append(copy,controls);program.appendChild(head);const heading=addText(document.createElement('h3'),dayOnly(state.day).toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'long',year:'numeric'}));heading.className='channels2-day-heading';program.appendChild(heading);const events=document.createElement('section');events.className='channels2-events';const now=Math.floor(Date.now()/1000);if(!state.events.length){const empty=addText(document.createElement('div'),'Für diesen Tag wurden keine EPG-Einträge gefunden.');empty.className='channels2-empty';events.appendChild(empty);}state.events.forEach(event=>{const button=document.createElement('button');button.type='button';button.className='channels2-event';if(eventStart(event)<=now&&eventEnd(event)>now)button.classList.add('current');if(state.event===event)button.classList.add('active');const time=addText(document.createElement('span'),clock(eventStart(event))+'–'+clock(eventEnd(event)));time.className='channels2-time';const copy=document.createElement('span');const title=addText(document.createElement('span'),eventTitle(event));title.className='channels2-title';copy.appendChild(title);if(eventSubtitle(event)){const sub=addText(document.createElement('span'),eventSubtitle(event));sub.className='channels2-meta';copy.appendChild(sub);}const badge=addText(document.createElement('span'),eventStart(event)<=now&&eventEnd(event)>now?'Jetzt':'EPG');badge.className='channels2-badge';button.append(time,copy,badge);button.onclick=()=>{state.event=event;render();};events.appendChild(button);});program.appendChild(events);renderDetail(program);};search.oninput=()=>{state.filter=search.value.trim();filterChannels();renderChannels();status.textContent=state.visible.length+' von '+state.channels.length+' Kanälen sichtbar.';};reload.onclick=loadChannels;renderChannels();renderProgram();}
  function loadEvents(){const api=clientApi();if(!api||!state.channel){render();return;}const start=dayOnly(state.day),end=moveDay(start,1),range={start:Math.floor(start.getTime()/1000),end:Math.floor(end.getTime()/1000)},sequence=++state.sequence;const cached=typeof api.fetchClientEpgCacheWindow==='function'?api.fetchClientEpgCacheWindow({query:{backend:backendId(),channelId:channelId(state.channel),fromTime:String(range.start),untilTime:String(range.end),limit:'0',_:String(Date.now())},cache:'no-store',credentials:'same-origin'}).catch(()=>({events:[]})):Promise.resolve({events:[]});cached.then(data=>list(data,'events').length||typeof api.fetchClientEpgChannelWindow!=='function'?data:api.fetchClientEpgChannelWindow({query:{channelId:channelId(state.channel),from:String(range.start),timespan:String(range.end-range.start),limit:'192',_:String(Date.now())},cache:'no-store',credentials:'same-origin'})).then(data=>{if(!state.active||sequence!==state.sequence)return;state.events=list(data,'events').filter(event=>{const id=eventChannelId(event);return(!id||id===channelId(state.channel))&&eventStart(event)<range.end&&eventEnd(event)>range.start;}).sort((a,b)=>eventStart(a)-eventStart(b));state.event=null;render();}).catch(error=>{const target=mount();if(target&&state.active){const message=addText(document.createElement('p'),'Tagesprogramm konnte nicht geladen werden: '+error.message);message.className='channels2-status error';target.prepend(message);}});}
  function loadChannels(){const api=clientApi(),target=mount();if(!target)return;target.replaceChildren();const loading=addText(document.createElement('p'),'Lade Channels 2 …');loading.className='channels2-status';target.appendChild(loading);if(!api||typeof api.fetchClientChannels!=='function'){loading.className='channels2-status error';loading.textContent='Kanäle konnten nicht geladen werden: Client API ist nicht verfügbar.';return;}api.fetchClientChannels({query:{backend:backendId(),_:String(Date.now())},cache:'no-store',credentials:'same-origin'}).then(data=>{if(!state.active)return;state.channels=list(data,'channels').map(c=>Object.assign({},c,{id:channelId(c),name:channelName(c),number:channelNumber(c),group:channelGroup(c)})).sort((a,b)=>channelNumber(a)-channelNumber(b)||channelName(a).localeCompare(channelName(b),'de-DE'));filterChannels();if(!state.channel&&state.channels.length)state.channel=state.channels[0];render();if(state.channel)loadEvents();}).catch(error=>{if(state.active){loading.className='channels2-status error';loading.textContent='Kanäle konnten nicht geladen werden: '+error.message;}});}
  const moduleApi=Object.freeze({activate(){state.active=true;loadChannels();},deactivate(){state.active=false;state.sequence+=1;},refresh(){if(state.active)loadChannels();}});const runtime=platform();if(runtime&&typeof runtime.registerModule==='function'&&(!runtime.hasModule||!runtime.hasModule('channels2')))runtime.registerModule('channels2',moduleApi);global.VdrSuiteChannels2=moduleApi;
}(window));
