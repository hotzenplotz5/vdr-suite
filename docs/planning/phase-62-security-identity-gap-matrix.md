# Phase 62 Security and Identity Gap Matrix

Status: active Phase 62 planning and implementation matrix  
Repository baseline: `cb77ff66e11dca7db2eafa36525762dcde35102d` (`main`, merge of PR #115)  
Implemented branch slices: Phase 62 Slice 1 plus the persistent lifecycle foundation of Slice 2

This matrix separates repository runtime truth from accepted architecture contracts. A document or ADR entry is not counted as implemented runtime.

## Start-state findings

- `TestHttpServer` authenticated every request by comparing one complete `Authorization` header value. The default value was embedded in the runtime.
- The result of that comparison was not represented as an actor, device, session, principal, permission, scope, or authorization decision.
- `BackendAccessPolicy` already enforced backend existence, enabled state, and read-only state. That is a backend safety policy, not actor authorization.
- Mutating services already contain useful safety primitives, including backend scope, capability checks, operation IDs in several workflows, selected duplicate prevention, and selected readback verification.
- There was no durable append-only implementation of ADR-0049's `AccountabilityEvent`.
- The unversioned `/api/...` routes remain compatibility routes. Phase 62 does not publish `/api/v1` and does not claim Phase 67.
- ADR-0051 is not part of this baseline. It remains proposed in Draft PR #116 and is treated only as client-consumer context.

## Gap matrix

| Security area | Current runtime state | Current owner/component | Affected routes and mutations | Accepted ADR direction | Concrete gap | Risk | Planned Phase 62 slice | Test evidence | Documentation evidence | Dependencies | Later phase not pulled forward |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Principal / actor model | Canonical actor and request context plus persisted actor lifecycle state | `SecurityIdentity.h`, `SecurityIdentityRepository`, `PersistentIdentityResolver` | All HTTP requests; first enforced mutation is remote control | ADR-0013, ADR-0041, ADR-0049 | Only the legacy local actor is currently authenticated; managed user/service/agent issuance remains open | Actions cannot yet be attributed to independently enrolled users | Slice 1 model; Slice 2 persistence implemented, issuance still open | authorization, repository, resolver, HTTP-gate tests | Slice 1, Slice 2 and architecture documents | Existing HTTP request headers and SQLite | Phase 63 agent deployment |
| User identity | Compatibility user is persisted with type, display name, active state and revocation timestamp | `security_actors`, `LegacyBasicAuthenticator`, resolver | Local browser and clients presenting legacy Basic credential | ADR-0013, ADR-0041 | No user-management API, password lifecycle, enrollment, recovery or separate per-user credentials | Shared credential still prevents individual accountability | Remaining Slice 2 | Persistent actor lookup and revocation-capable repository tests | Slice 2 document | Production authentication design | No Android application |
| Device identity | Device record is persisted, actor-bound, resolved per request and revocable | `security_devices`, resolver | Protected remote mutation | ADR-0041, ADR-0049 | No device enrollment, trust level, key material or protected management operation | Current shared credential cannot yet prove possession by one physical device | Slice 2 persistence implemented; management remains | repository binding and HTTP-gate tests | Architecture and Slice 2 documents | User credential issuance | Phase 63 agent trust runtime |
| Session identity | Session record is persisted, actor/device-bound, expirable and revocable | `security_sessions`, resolver, `AuthorizationService` | Protected remote mutation | ADR-0041 | No issued browser session, refresh, logout endpoint or cleanup process; compatibility session is deterministic | Compatibility credential remains effectively long-lived until server-side lifecycle change | Remaining Slice 2 | expiry and revocation negatives | Slice 2 document | Browser authentication contract | Phase 67 public client contract |
| Credential identity | Credential metadata is persisted with actor binding, type, expiry, revocation and rotation predecessor field | `security_credentials`, resolver | Authenticated HTTP requests | ADR-0041, ADR-0049 | Repository stores no secret and no production verifier/issuer exists yet | Shared Basic secret remains unsuitable for remote exposure | Slice 2 persistence implemented; secure issuance/verification remains | repository lifecycle and revoked-credential HTTP negative | Slice 2 document | Password/token mechanism decision | Phase 67 stable public authentication |
| Authentication mechanism | Explicit compatibility authenticator followed by persistent server-side lifecycle resolution | `SecurityConfiguration`, `LegacyBasicAuthenticator`, `PersistentIdentityResolver` | All HTTP requests | ADR-0041, ADR-0048 | No password hash verifier, cookie/CSRF flow, native token issuance, rotation or MFA | Compatibility Basic remains unsuitable for public exposure | Remaining Slice 2 | missing, invalid, allowed, persisted-revoked tests | Architecture and Slice 2 docs | Identity repositories now available | Phase 67 public authentication contract |
| Session lifecycle | Persisted expiry and revocation are enforced on every authenticated request | repository, resolver, authorization service | Protected mutations | ADR-0041 | No creation/refresh/logout/cleanup administration flow | Lifecycle can be enforced but not safely operated through a protected API | Remaining Slice 2 and Slice 3 admin policy | repository expiry and resolver negative | Slice 2 document | Admin permissions | None |
| Revocation | Actor/device/session/credential state is persisted and fail-closed; repeated bootstrap does not reactivate records | repository and resolver | Protected mutations and compatibility access | ADR-0041, ADR-0049 | No protected revoke/reactivate command or authentication lifecycle event | Direct database operation is not a final administration contract | Remaining Slice 2 plus Slice 3 administration | revoked credential at HTTP gate; policy negatives for other identity types | Slice 2 and architecture docs | Admin permission catalogue | No Phase 63 operations |
| Roles | Not implemented | No runtime owner | All mutations and protected reads | ADR-0013 | No role definitions or actor-role assignment | Permission management does not scale | Slice 3 | Pending role-assignment tests | This matrix | User repository exists | None |
| Permissions | Central exact or wildcard permission matching; `remote.control` is first permission | `AuthorizationService` | `POST /api/vdr/remote/actions` | ADR-0013, ADR-0041 | Other mutation permissions are not migrated and grants are not persisted | Compatibility actor still has wildcard rights by default | Slice 1 first route; Slice 3 complete catalogue | allowed and missing-permission tests | Architecture document | Route migration inventory | Phase 64 timer orchestration |
| Backend scopes | Exact backend or wildcard grants are evaluated centrally | `PermissionGrant`, `AuthorizationService` | Protected remote mutation uses request `backendId` | ADR-0013, ADR-0041, ADR-0042 | No persisted scope assignments; other routes still rely only on backend safety policy | Actor can otherwise affect unintended sites | Slice 3 | wrong-backend negative | Architecture document | Backend IDs and role/grant persistence | Phase 63 remote-site runtime |
| Central server authorization | Evaluates before router dispatch; enforced mode rejects unmigrated POST routes | `SecurityHttpGate` | First route: remote action | ADR-0013, ADR-0041, ADR-0042 | Remaining mutations and sensitive reads need explicit mappings | New route without policy could become writable | Slice 3 migration | anonymous, invalid, revoked, forbidden, wrong-scope, allowed and unmigrated tests | Slice documents | Mutation inventory | No shadow API |
| Backend read-only enforcement | Implemented independently of actor permissions | `BackendAccessPolicy`, mutation services | Remote, timer, recording, channel, SearchTimer workflows | ADR-0013, ADR-0041 | Must remain a separate hard boundary | Actor permission could otherwise bypass site policy | Preserved in every slice | Existing backend policy tests plus full suite | Existing architecture docs | Backend registry | Phase 63 site operation |
| Mutation preconditions | Selected workflows have validation and safety gates | Recording, timer, channel, SearchTimer services | Recording execute; timer actions; channel move; SearchTimer mutations | ADR-0042 | No universal envelope or common decision order | Inconsistent conflict and retry behavior | Slice 4 | Pending common contract tests | This matrix | Route migration and operation model | Phase 64 timer orchestration |
| Revisions | Selected domain revisions/generations exist; no universal HTTP contract | Existing mutation services | Timer, recording, channel, SearchTimer mutations | ADR-0042, ADR-0048 | `If-Match` / expected revision is not consistently accepted, checked and returned | Lost updates and stale writes | Slice 4 | Pending revision negatives | This matrix | Shared mutation envelope | Phase 67 stable public API |
| Idempotency keys | Selected operation IDs and duplicate controls exist; no common header contract | Existing action/workflow services | Remote and selected recording/SearchTimer operations | ADR-0042, ADR-0048 | `Idempotency-Key` is not universal, persisted or conflict-checked | Retries can duplicate side effects | Slice 4 | Pending replay tests | This matrix | Durable operation repository | Phase 64/65 orchestration |
| Operation IDs | Remote requires `operationId` and propagates it into authorization audit | Remote action domain and gate | `POST /api/vdr/remote/actions` | ADR-0042, ADR-0049 | Other routes use inconsistent identifiers; no shared lifecycle store | Cross-system tracing and retry resolution incomplete | Slice 4 | audit operation context test | Architecture document | Request parser and audit repository | Phase 65 media sessions |
| Accountability event model | Append-only SQLite rows and update/delete blockers | `AccountabilityEventRepository` | Authentication failures and remote authorization decisions | ADR-0049 | Partial catalogue; no transactional outbox, retention, query API, export or mutation completion event | Incomplete forensic evidence | Slice 5 | append-only and allow/deny tests | Architecture document | SQLite and request context | Phase 67 public audit API |
| Audit actor and device | Actor, actor type, device, session, auth state, request, correlation, backend, operation, decision, reason and outcome persist | `SecurityHttpGate` | Protected remote mutation and compatibility denial | ADR-0049 | Credential ID and lifecycle event catalogue are not yet audit fields/events; native identities not issued | Audit still identifies compatibility identity | Slice 5 after identity issuance | audit field assertions | Architecture document | Identity lifecycle repository | Phase 63 agents |
| Audit failure semantics | Allowed protected mutation returns 503 if pre-dispatch audit append fails | gate | Remote action | ADR-0042, ADR-0049 | No transactional outbox joining domain state and completion evidence | Other mutations can still dispatch without Phase 62 audit | Slice 5 | audit-unavailable negative | Slice 1 document | Durable SQLite | No later domain execution |
| Security error codes | Nested machine-readable errors with request ID and `no-store`; includes credential/session lifecycle codes | gate | Authentication and authorization failures | ADR-0048 | Other routes retain ad-hoc errors | Mixed client error shapes during migration | Slice 3/4 convergence | error-code and no-secret assertions | Architecture document | HTTP response model | Phase 67 stable errors |
| Browser compatibility | Default `legacy-basic` preserves current browser; mapped lifecycle is now persisted | configuration, authenticator, repository, resolver | Existing frontend GETs and POSTs | ADR-0041 controlled compatibility | Shared credential and deterministic compatibility session remain | Accidental exposure grants broad control | Remaining Slice 2, then controlled retirement | frontend/full suite and gate tests | Slice 2 document | Existing browser credential | Phase 67 client release |
| Native client compatibility | Identity, request IDs, scoped grants and machine errors are transport-neutral | security domain | Unversioned compatibility API only | ADR-0048; proposed ADR-0051 not accepted on main | No production token/session API and no stable `/api/v1` | Native clients cannot rely on public stable contract | Slice 2-5 prepare contracts | Pending issuance tests | This matrix | Identity/session runtime | Phase 67 API/SDK |
| Administrative operations | Existing cache/native-fuzzy routes have no actor-specific admin permission | router/controllers | EPG cache refresh, preview refresh, native fuzzy refresh, stale-probe delete | ADR-0013, ADR-0049 | No `admin.*` catalogue or protected lifecycle management | High-impact operations share broad credential | Slice 3 | Pending admin allow/deny tests | This matrix | Roles and permissions | No Phase 63 operations |
| Credential/token leak prevention | Authorization value is never persisted or reflected; identity repository stores only credential ID and lifecycle metadata | authenticator, repository, gate, audit | All security failures | ADR-0041, ADR-0048, ADR-0049 | Repository-wide log redaction and future browser storage policy still pending | Future code could leak secrets | Slice 2 guard plus later hardening | invalid secret absence and architecture guard | Architecture and Slice 2 docs | Logging inventory | Phase 67 hardening |

## Mutating and stateful POST inventory

The following is the Phase 62 migration inventory derived from `ApiRouter` and the live-remote runtime. Validation, preview and planning POSTs are listed because HTTP method alone cannot decide whether a request mutates state.

| Route family | Runtime owner | Classification at Phase 62 start | Phase 62 migration target |
|---|---|---|---|
| `/api/vdr/remote/actions` | `LiveRemoteApiRuntime`, `RemoteActionController`, `RemoteActionService` | Mutating; operation ID, allowlist, capability and backend write policy already exist | Migrated in Slice 1 to `remote.control@backend` with persisted identity resolution and pre-dispatch accountability |
| `/api/recordings/actions/execute` and `/api/vdr/recordings/actions/execute` | Recording action execution controller/service | Mutating | Slice 3 permission, then Slice 4 revision/idempotency |
| Recording validate/preview aliases | Recording validation/preview controllers | Non-mutating POST | Explicit read/preview policy in Slice 3 |
| `/api/vdr/timers/actions/create`, `/update`, `/delete` | Timer action services | Mutating | Slice 3 permissions; Slice 4 revisions/idempotency; Phase 64 retains orchestration |
| `/api/vdr/channels/move` and alias | Channel move service | Mutating | Slice 3 permission; Slice 4 revision/idempotency |
| SearchTimer create/update/delete/execute aliases | SearchTimer services | Mixed mutating workflow | Slice 3 permission catalogue; Slice 4 operation contract |
| SearchTimer validate/plan/preview aliases | SearchTimer planning services | Non-mutating POST | Explicit safe classification in Slice 3 |
| `/api/epg/cache/refresh` | EPG cache controller | Administrative state change | Slice 3 `admin.epg.refresh` |
| SearchTimer preview-cache refresh aliases | Preview cache controller | Administrative cache mutation | Slice 3 admin permission |
| Native fuzzy refresh aliases | Native fuzzy operator controller | Administrative probe/refresh mutation | Slice 3 admin permission |
| Native fuzzy stale-probe delete aliases | Stale-probe controller | Administrative deletion | Slice 3 admin permission |

## Phase 62 slice order

1. **Slice 1 Security identity foundation and first protected mutation — implemented and real-runtime validated**  
   Canonical request identity, central permission/scope decision, explicit legacy adapter, Remote enforcement, request/correlation IDs and append-only pre-dispatch evidence.
2. **Slice 2 Persistent identities, credentials, devices, sessions and revocation — persistence foundation implemented, issuance/management still open**  
   Implemented: repositories, compatibility bootstrap, expiry/revocation state and request-time resolution. Still required: secure per-user/native issuance and verification, logout/rotation/recovery, browser cookie/CSRF contract and protected lifecycle administration.
3. **Slice 3 Roles, permissions, scopes and complete mutation-route migration**  
   Permission catalogue, role assignment, administrative permissions and explicit classification of non-mutating POST routes.
4. **Slice 4 Common mutation envelope, revisions, idempotency and operation lifecycle**  
   Shared preconditions, `If-Match`, `Idempotency-Key`, replay semantics, conflict codes and durable operations.
5. **Slice 5 Complete accountability catalogue, transactional outbox and protected queries**  
   Mutation outcomes, authentication lifecycle events, export/retention, protected audit reads and fail-before-dispatch guarantees.
6. **Slice 6 Compatibility retirement readiness and Phase 62 closeout**  
   Migration controls, negative end-to-end suite, documentation truth and evidence that Phase 63-67 runtime was not pulled forward.

Phase 62 remains open. Slice 1 is runtime validated; Slice 2 has an implemented persistence/revocation foundation but still requires secure issuance and protected lifecycle management before closeout.
