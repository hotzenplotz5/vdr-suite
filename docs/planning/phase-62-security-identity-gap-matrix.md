# Phase 62 Security and Identity Gap Matrix

Status: active Phase 62 planning and implementation matrix  
Repository baseline: `cb77ff66e11dca7db2eafa36525762dcde35102d` (`main`, merge of PR #115)  
Implemented branch slices: Phase 62 Slice 1 plus real-runtime-accepted persistent lifecycle and first managed credential-verifier increments of Slice 2

This matrix separates repository runtime truth from accepted architecture contracts. A document or ADR entry is not counted as implemented runtime.

## Start-state findings

- `TestHttpServer` authenticated every request by comparing one complete `Authorization` header value. The default value was embedded in the runtime.
- The result of that comparison was not represented as an actor, device, session, principal, permission, scope, or authorization decision.
- `BackendAccessPolicy` already enforced backend existence, enabled state, and read-only state. That is a backend safety policy, not actor authorization.
- Mutating services already contain useful safety primitives, including backend scope, capability checks, operation IDs in several workflows, selected duplicate prevention, and selected readback verification.
- There was no durable append-only implementation of ADR-0049's `AccountabilityEvent`.
- The unversioned `/api/...` routes remain compatibility routes. Phase 62 does not publish `/api/v1` and does not claim Phase 67.
- ADR-0051 is not part of this baseline. It remains proposed in Draft PR #116 and is treated only as client-consumer context.

## Gap matrix

| Security area | Current runtime state | Current owner/component | Affected routes and mutations | Accepted ADR direction | Concrete gap | Risk | Planned Phase 62 slice | Test evidence | Documentation evidence | Dependencies | Later phase not pulled forward |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Principal / actor model | Canonical request context, persisted lifecycle state, legacy actor, and one optional separately configured managed actor | `SecurityIdentity`, lifecycle/provisioning repositories, authenticators, resolver | All HTTP requests; first enforced mutation is Remote | ADR-0013, ADR-0041, ADR-0049 | No protected enrollment or general multi-user/service issuer | Managed identity still depends on server configuration | Slice 1 model; Slice 2 persistence and first verifier implemented | authorization, provisioning, verifier, resolver, HTTP-gate tests; real-VDR managed attribution | Slice documents and architecture | HTTP Basic, modular hash, SQLite | Phase 63 agent deployment |
| User identity | Legacy compatibility user plus optional managed Basic user can be persisted and authenticated independently | provisioning/lifecycle repositories, managed authenticator | Authenticated GET and migrated Remote path | ADR-0013, ADR-0041 | No user-management API, password-change flow, recovery, or arbitrary number of issued users | Operational identity management remains manual | Remaining Slice 2 | managed positive/negative tests and real-VDR GET/Remote acceptance | Slice 2 document | Protected lifecycle administration | No Android application |
| Device identity | Device records are persisted, actor-bound, resolved per request, and revocable | `security_devices`, provisioning repository, resolver | Protected requests | ADR-0041, ADR-0049 | No device enrollment, possession proof, trust level, or key material | Configured device ID is attribution, not strong device authentication | Remaining Slice 2 | binding/revocation tests and real-VDR managed device attribution | Architecture and Slice 2 | Session/native credential design | Phase 63 agent trust runtime |
| Session identity | Session records are persisted, actor/device-bound, expirable, and revocable; Basic sessions remain deterministic | `security_sessions`, resolver, authorization | Protected requests | ADR-0041 | No issued browser session, secure cookie, refresh, logout, or cleanup | Repeated Basic password submission is not final browser security | Remaining Slice 2 | expiry/revocation negatives and real-VDR managed session attribution | Slice 2 | Browser cookie/CSRF contract | Phase 67 public client contract |
| Credential identity | Metadata plus optional login-to-credential one-way hash verifier are persisted | `security_credentials`, `security_basic_credential_verifiers`, resolver | Authenticated requests | ADR-0041, ADR-0049 | No protected issuance, hash generation, password change, recovery, or native token verifier | Server configuration is still required to provision the first managed credential | Remaining Slice 2 | verifier/provisioning tests, revoked-credential tests, real-VDR `$6$` verifier acceptance | Slice 2 and architecture | Lifecycle administration | Phase 67 stable public auth |
| Authentication mechanism | Transitional legacy header comparison plus optional strict Basic parser and `crypt_r` verifier; persistent lifecycle resolution follows both | legacy/managed authenticators, verifier repository, resolver | All HTTP requests | ADR-0041, ADR-0048 | No cookie/CSRF flow, service/native token issuance, rotation, refresh, MFA, or recovery | Basic remains unsuitable for public remote exposure | Remaining Slice 2 | malformed, wrong-password, supported-hash, lifecycle and route-boundary tests; real-VDR 200/401 evidence | Architecture and Slice 2 | libcrypt and identity persistence | Phase 67 public auth contract |
| Session lifecycle | Persisted expiry and revocation are enforced per authenticated request | repository, resolver, authorization | Protected routes | ADR-0041 | No create/refresh/logout/cleanup administration flow | Lifecycle can be enforced but not safely operated via API | Remaining Slice 2 and Slice 3 admin policy | expiry and revocation negatives | Slice 2 | Admin permissions | None |
| Revocation | Actor/device/session/credential state is persisted and fail-closed; repeated bootstrap does not reactivate records | repositories and resolver | Protected routes and compatibility access | ADR-0041, ADR-0049 | No protected revoke/reactivate command or lifecycle event catalogue | Direct database/configuration operation is not final administration | Remaining Slice 2 plus Slice 3 | managed/legacy revoked-credential negatives and real-VDR legacy exercise | Slice 2 | Admin permission catalogue | No Phase 63 operations |
| Roles | Not implemented | No runtime owner | All mutations and protected reads | ADR-0013 | No role definitions or actor-role assignment | Permission management does not scale | Slice 3 | Pending | Matrix | Managed actors now representable | None |
| Permissions | Central exact/wildcard matching; legacy and managed identities can carry distinct transient grants; `remote.control` is first permission | `AuthorizationService`, configuration | `POST /api/vdr/remote/actions` | ADR-0013, ADR-0041 | Grants are not persisted and other routes are not migrated | Legacy actor still has wildcard compatibility rights | Slice 3 | allowed, missing-permission, scope and managed-boundary tests; real-VDR `remote.control@default` allow | Architecture | Route inventory and grant persistence | Phase 64 timer orchestration |
| Backend scopes | Exact backend or wildcard grants are evaluated centrally | `PermissionGrant`, authorization | Protected Remote request backend | ADR-0013, ADR-0041, ADR-0042 | No persisted scope assignments; most routes still use only backend safety policy | Actor may otherwise affect unintended sites | Slice 3 | wrong-backend negatives and real-VDR `default` scope evidence | Architecture | Backend IDs and roles | Phase 63 remote-site runtime |
| Central server authorization | Evaluates before router dispatch; managed identities and enforced mode cannot use unmigrated POSTs | `SecurityHttpGate` | First route: Remote; all other POSTs classified as legacy bypass or fail-closed | ADR-0013, ADR-0041, ADR-0042 | Remaining mutations and sensitive reads need explicit mappings | Legacy bypass remains until migration | Slice 3 | anonymous, invalid, revoked, forbidden, wrong-scope, managed-unmigrated and allowed tests; real-VDR Timer 503 and Remote 200 | Slice docs | Mutation inventory | No shadow API |
| Backend read-only enforcement | Implemented independently of actor permissions | `BackendAccessPolicy`, mutation services | Remote, Timer, Recording, Channel, SearchTimer | ADR-0013, ADR-0041 | Must remain a separate hard boundary | Actor permission could otherwise bypass site policy | Preserved in every slice | Existing policy/full tests | Existing docs | Backend registry | Phase 63 site operation |
| Mutation preconditions | Selected workflows have validation and safety gates | Recording, Timer, Channel, SearchTimer services | Mutations | ADR-0042 | No universal envelope or decision order | Inconsistent conflict/retry behavior | Slice 4 | Pending | Matrix | Route migration | Phase 64 orchestration |
| Revisions | Selected domain revisions/generations exist; no universal HTTP contract | Existing mutation services | Mutable resources | ADR-0042, ADR-0048 | `If-Match` / expected revision inconsistent | Lost updates and stale writes | Slice 4 | Pending | Matrix | Shared envelope | Phase 67 API |
| Idempotency keys | Selected operation IDs/duplicate controls; no common header or durable replay contract | Existing action/workflow services | Remote and selected workflows | ADR-0042, ADR-0048 | `Idempotency-Key` not universal or conflict-checked | Retries can duplicate effects | Slice 4 | Pending | Matrix | Durable operations | Phase 64/65 |
| Operation IDs | Remote requires `operationId` and audits it | Remote domain and gate | Remote POST | ADR-0042, ADR-0049 | Other routes inconsistent; no common lifecycle store | Retry resolution incomplete | Slice 4 | operation-context tests and real-VDR operation `phase62-managed-1785212278` | Architecture | Operation repository | Phase 65 media sessions |
| Accountability event model | Append-only allow/deny rows with update/delete blockers | `AccountabilityEventRepository` | Authentication failures and Remote authorization | ADR-0049 | Partial catalogue; no outbox, retention, query/export, or mutation completion | Incomplete forensic evidence | Slice 5 | append-only/gate tests and real-VDR invalid, unmigrated and allowed rows | Architecture | SQLite/request context | Phase 67 audit API |
| Audit actor and device | Verified actors, including managed actor, can be attributed with device/session/auth state | gate | Protected Remote and access denials | ADR-0049 | Credential ID and auth lifecycle events are not yet first-class audit fields/events | Audit catalogue remains partial | Slice 5 | field tests and real-VDR managed actor/device/session rows | Architecture | Identity issuance | Phase 63 agents |
| Audit failure semantics | Required pre-dispatch append failure returns 503 | gate | Remote | ADR-0042, ADR-0049 | No transactional outbox joining domain result and completion evidence | Other mutations may still dispatch without Phase 62 audit | Slice 5 | audit-unavailable test | Slice 1 | Durable persistence | None |
| Security error codes | Machine-readable nested errors with request ID and `no-store`; invalid enforced GET is no longer downgraded to anonymous | gate | Security failures | ADR-0048 | Other routes retain ad-hoc errors | Mixed client shapes | Slice 3/4 convergence | error/no-secret tests and real-VDR 401/503 evidence | Architecture | HTTP response model | Phase 67 stable errors |
| Browser compatibility | Legacy browser remains default; optional managed Basic identity can coexist but has no legacy POST bypass | configuration/authenticators/gate | Existing frontend and managed requests | ADR-0041 controlled migration | Shared legacy credential and deterministic sessions remain | Public exposure remains unsafe | Remaining Slice 2 then Slice 6 | frontend/full/gate tests and real-VDR coexistence | Slice 2 | Browser session migration | Phase 67 release |
| Native client compatibility | Identity, verifier, request IDs, scoped grants, and errors are transport-neutral | security domain | Unversioned compatibility API | ADR-0048; proposed ADR-0051 not accepted | No native token/session API or stable `/api/v1` | Native clients cannot rely on final contract | Slice 2-5 preparation | Pending native issuance | Matrix | Session/token runtime | Phase 67 API/SDK |
| Administrative operations | Existing admin-like routes lack actor-specific permissions; no security-management API | router/controllers | Cache refresh, fuzzy operations, future identity lifecycle | ADR-0013, ADR-0049 | No `admin.*` catalogue or protected lifecycle management | High-impact operations remain under legacy broad credential | Slice 3 | Pending | Matrix | Roles/permissions | None |
| Credential/token leak prevention | Submitted headers/passwords are not persisted or reflected; managed verifier stores only a one-way modular hash | authenticators, verifier/lifecycle repositories, gate, audit | All security paths | ADR-0041, ADR-0048, ADR-0049 | Repository-wide log redaction and future browser storage policy remain | Future code could leak secrets | Slice 2 guard plus hardening | wrong-password/no-secret tests, architecture guards and real-VDR inspection | Architecture/Slice 2 | Logging inventory | Phase 67 hardening |

## Mutating and stateful POST inventory

| Route family | Runtime owner | Classification at Phase 62 start | Phase 62 migration target |
|---|---|---|---|
| `/api/vdr/remote/actions` | Remote runtime/controller/service | Mutating with operation ID, allowlist, capability, backend policy | Migrated in Slice 1 with persisted identity resolution; managed identities require `remote.control@backend` |
| Recording execute aliases | Recording action services | Mutating | Slice 3 permission, Slice 4 revision/idempotency |
| Recording validate/preview aliases | Validation/preview | Non-mutating POST | Explicit safe policy in Slice 3 |
| Timer create/update/delete | Timer services | Mutating | Slice 3 permissions; Slice 4 contracts; Phase 64 retains orchestration |
| Channel move aliases | Channel service | Mutating | Slice 3 permission; Slice 4 contracts |
| SearchTimer create/update/delete/execute | SearchTimer services | Mixed mutation workflow | Slice 3 permissions; Slice 4 operation contract |
| SearchTimer validate/plan/preview | Planning services | Non-mutating POST | Explicit safe classification in Slice 3 |
| EPG/cache/native-fuzzy administration | Admin controllers/services | Administrative state change | Slice 3 `admin.*` permissions |

## Phase 62 slice order

1. **Slice 1 Security identity foundation and first protected mutation — implemented and real-runtime validated**  
   Canonical context, central permission/scope decision, explicit legacy adapter, Remote enforcement, request/correlation IDs, and append-only pre-dispatch evidence.
2. **Slice 2 Persistent identities, credentials, devices, sessions and revocation — lifecycle foundation and first managed verifier implemented and real-runtime accepted**  
   Implemented and accepted: persistence, request-time lifecycle resolution, separate managed identity provisioning, strict Basic parsing, one-way crypt verifier, no managed legacy-POST bypass, secret-safe invalid-password handling, and backend-scoped Remote authorization. Still required: protected issuance/hash generation, browser cookie/CSRF sessions, native/service credentials, logout/rotation/recovery, and lifecycle administration.
3. **Slice 3 Roles, permissions, scopes and complete route migration**  
   Permission catalogue, persistent roles/grants/scopes, administrative permissions, and explicit safe POST classification.
4. **Slice 4 Common mutation envelope, revisions, idempotency and operation lifecycle**  
   Shared preconditions, `If-Match`, `Idempotency-Key`, replay/conflict semantics, and durable operations.
5. **Slice 5 Complete accountability catalogue, transactional outbox and protected queries**  
   Authentication/mutation outcomes, outbox, protected audit reads, export, and retention.
6. **Slice 6 Compatibility retirement readiness and Phase 62 closeout**  
   Migration controls, negative end-to-end suite, documentation truth, and proof that Phase 63-67 runtime was not pulled forward.

Phase 62 remains open. The next strict work remains inside Slice 2: secure browser session/cookie and CSRF behavior plus protected lifecycle administration, not Android or later-phase runtime.