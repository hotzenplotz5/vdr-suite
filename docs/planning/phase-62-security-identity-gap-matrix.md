# Phase 62 Security and Identity Gap Matrix

Status: active Phase 62 planning and implementation matrix  
Repository baseline: `cb77ff66e11dca7db2eafa36525762dcde35102d` (`main`, merge of PR #115)  
Implemented branch slices: Phase 62 Slice 1; real-runtime-accepted lifecycle and managed Basic increments of Slice 2; staged browser-session credential/cookie/CSRF verifier foundation not yet wired into the HTTP runtime

This matrix separates repository runtime truth from accepted architecture contracts. A document, staged component, or ADR entry is not counted as active request-path runtime unless it is connected and tested through `TestHttpServer` and `SecurityHttpGate`.

## Start-state findings

- `TestHttpServer` authenticated every request by comparing one complete `Authorization` header value. The default value was embedded in the runtime.
- The result of that comparison was not represented as an actor, device, session, principal, permission, scope, or authorization decision.
- `BackendAccessPolicy` already enforced backend existence, enabled state, and read-only state. That is a backend safety policy, not actor authorization.
- Mutating services already contain useful safety primitives, including backend scope, capability checks, operation IDs in several workflows, selected duplicate prevention, and selected readback verification.
- There was no durable append-only implementation of ADR-0049's `AccountabilityEvent`.
- The unversioned `/api/...` routes remain compatibility routes. Phase 62 does not publish `/api/v1` and does not claim Phase 67.
- ADR-0051 is not part of this baseline. It remains proposed in Draft PR #116 and is treated only as client-consumer context.

## Gap matrix

| Security area | Current runtime state | Current owner/component | Affected routes and mutations | Accepted ADR direction | Concrete gap | Risk | Planned Phase 62 slice | Test evidence | Documentation evidence | Dependencies | Later phase not pulled forward |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Principal / actor model | Canonical request context, persisted lifecycle state, legacy actor, and one optional separately configured managed actor; staged browser sessions bind to the same actor model | `SecurityIdentity`, lifecycle/provisioning repositories, Basic authenticators, staged browser-session repository/authenticator, resolver | Current Basic request path; future browser-cookie path | ADR-0013, ADR-0041, ADR-0049 | No protected enrollment or general multi-user/service issuer | Managed identity and future browser issuance still depend on controlled server operation | Slice 1 model; Slice 2 persistence, Basic verifier and browser-session foundation | authorization, provisioning, verifier, resolver and gate tests; browser-session verifier matrix; real-VDR managed attribution | Slice documents and architecture | modular hashes, SQLite, secure randomness for issuance | Phase 63 agent deployment |
| User identity | Legacy compatibility and optional managed Basic users authenticate independently; browser-session records can bind an issued browser identity but are not HTTP-issued | provisioning/lifecycle repositories, managed authenticator, staged browser-session repository/authenticator | Authenticated GET and migrated Remote today; future browser login/session | ADR-0013, ADR-0041 | No protected user-management API, password change/recovery, arbitrary issued users, or browser login | Operational identity management remains manual | Remaining Slice 2 | managed real-VDR tests; staged browser-session positive/negative tests | Slice 2 | Protected lifecycle administration and issuance | No Android application |
| Device identity | Device records are persisted, actor-bound, resolved per Basic request, and revocable; staged browser credentials bind to a device ID | `security_devices`, provisioning repository, resolver, browser-session repository | Protected Basic requests; future browser-cookie requests | ADR-0041, ADR-0049 | No device enrollment, possession proof, trust level, or automatic browser-device identity creation | Configured/bound device ID is attribution, not strong device authentication | Remaining Slice 2 | binding/revocation tests, real-VDR managed device attribution, browser-session binding test | Architecture and Slice 2 | Session issuance/native credential design | Phase 63 agent trust runtime |
| Session identity | Basic session rows are persistent and deterministic; staged browser-session verifier has token ID, one-way secret hash, expiry and revocation but is not in the runtime request path | `security_sessions`, resolver, `security_browser_session_credentials`, `BrowserSessionAuthenticator` | Current protected Basic requests; future cookie requests | ADR-0041 | No atomic HTTP issuance, secure cookie attributes, refresh, logout, idle expiry or cleanup | Repeated Basic password submission remains current browser behavior; staged verifier alone is not a deployable session flow | Remaining Slice 2 | lifecycle negatives, real-VDR managed session attribution, browser-session cookie/expiry/revocation tests | Slice 2 and architecture | Secure randomness, login/logout, Gate integration | Phase 67 public client contract |
| Credential identity | Basic metadata/verifier is persisted and real-runtime accepted; staged browser credential binds issuing credential plus one-way session/CSRF hashes | `security_credentials`, `security_basic_credential_verifiers`, `security_browser_session_credentials`, resolver | Current Basic requests; future browser-cookie requests | ADR-0041, ADR-0049 | No protected atomic issuance, managed password change/recovery, or native/service verifier | Server configuration still provisions the first managed credential; manual creation must not become the final browser flow | Remaining Slice 2 | Basic verifier tests/real-VDR evidence; browser-session schema, plaintext-negative and verification tests | Slice 2 and architecture | Lifecycle administration and secure issuer | Phase 67 stable public auth |
| Authentication mechanism | Runtime uses transitional legacy comparison plus optional strict managed Basic verifier; staged strict cookie verifier exists but is not connected | legacy/managed authenticators, Basic verifier repository, resolver; staged browser-session authenticator/repository | All current HTTP requests; future cookie requests | ADR-0041, ADR-0048 | No login/logout, `Set-Cookie`, authentication precedence, Gate wiring, service/native token issuance, refresh, MFA or recovery | Basic remains unsuitable for public remote exposure; enabling staged code without issuance/policy would be unsafe | Remaining Slice 2 | Basic malformed/wrong/lifecycle/real-VDR tests; browser cookie wrong/unknown/malformed/duplicate/expired/revoked matrix | Architecture and Slice 2 | libcrypt, identity persistence, secure random generation | Phase 67 public auth contract |
| CSRF protection | Independent staged `X-CSRF-Token` verifier is bound to a valid active browser-session record; no real mutation route enforces it yet | `BrowserSessionAuthenticator` | Future browser-authenticated POST mutations | ADR-0041, ADR-0048 | No HTTP issuance of CSRF secret, frontend delivery contract, Gate enforcement, error/audit catalogue, or rotation | Cookie authentication without Gate-level CSRF would permit cross-site mutation attempts | Remaining Slice 2 | correct, missing and wrong CSRF tests plus expired/revoked session negatives | Slice 2 and architecture | Browser-session issuance and Gate integration | No Phase 67 public API claim |
| Session lifecycle | Persisted Basic expiry/revocation is enforced per request; staged browser verifier independently enforces active/expiry/revocation | identity repository/resolver; browser-session repository/authenticator | Current Basic protected routes; future cookie routes | ADR-0041 | No atomic create/refresh/logout/cleanup administration flow or coupled revocation of session and credential rows | Lifecycle primitives can be tested but not yet safely operated through HTTP | Remaining Slice 2 and Slice 3 admin policy | Basic expiry/revocation negatives; browser-session expiry/revoke tests | Slice 2 | Admin permissions and transactional issuance | None |
| Revocation | Actor/device/session/credential state is persistent and fail-closed for Basic; staged browser verifier supports persistent browser-session revocation | repositories and resolver/authenticators | Current protected Basic routes; future browser cookie access | ADR-0041, ADR-0049 | No protected revoke/reactivate/logout command, coupled browser lifecycle operation, or full event catalogue | Direct database/configuration operation is not final administration | Remaining Slice 2 plus Slice 3 | managed/legacy revoked negatives, real-VDR legacy exercise, browser-session revoke test | Slice 2 | Admin permission catalogue | No Phase 63 operations |
| Roles | Not implemented | No runtime owner | All mutations and protected reads | ADR-0013 | No role definitions or actor-role assignment | Permission management does not scale | Slice 3 | Pending | Matrix | Managed/browser actors now representable | None |
| Permissions | Central exact/wildcard matching; legacy and managed identities carry distinct transient grants; staged browser authenticator accepts configured grants but is not runtime wired | `AuthorizationService`, configuration, staged browser authenticator | `POST /api/vdr/remote/actions`; future cookie requests | ADR-0013, ADR-0041 | Grants are not persisted; browser issuance has no grant source; other routes are not migrated | Legacy actor still has wildcard compatibility rights | Slice 3 | allowed, missing-permission, scope and managed-boundary tests; real-VDR allow; staged browser context test | Architecture | Route inventory and grant persistence | Phase 64 timer orchestration |
| Backend scopes | Exact backend or wildcard grants are evaluated centrally; staged browser context can carry the same grant type | `PermissionGrant`, authorization | Protected Remote request backend; future cookie requests | ADR-0013, ADR-0041, ADR-0042 | No persisted scope assignments; browser-session grant loading and most routes remain open | Actor may otherwise affect unintended sites | Slice 3 | wrong-backend negatives, real-VDR `default` scope, staged browser context grant | Architecture | Backend IDs and roles | Phase 63 remote-site runtime |
| Central server authorization | Evaluates current Basic identities before router dispatch; managed/enforced mode cannot use unmigrated POSTs; staged cookie verifier is outside Gate | `SecurityHttpGate` | First route Remote; other POSTs legacy bypass or fail-closed | ADR-0013, ADR-0041, ADR-0042 | Cookie authentication precedence, CSRF decision, remaining mappings and sensitive reads need integration | Legacy bypass remains; staged browser session provides no protection until connected | Slice 2 integration then Slice 3 | current gate matrix and real-VDR Timer/Remote evidence; staged verifier unit tests only | Slice docs | Mutation inventory and browser integration | No shadow API |
| Backend read-only enforcement | Implemented independently of actor permissions | `BackendAccessPolicy`, mutation services | Remote, Timer, Recording, Channel, SearchTimer | ADR-0013, ADR-0041 | Must remain a separate hard boundary for all future cookie/native paths | Actor permission could otherwise bypass site policy | Preserved in every slice | Existing policy/full tests | Existing docs | Backend registry | Phase 63 site operation |
| Mutation preconditions | Selected workflows have validation and safety gates | Recording, Timer, Channel, SearchTimer services | Mutations | ADR-0042 | No universal envelope or decision order | Inconsistent conflict/retry behavior | Slice 4 | Pending | Matrix | Route migration | Phase 64 orchestration |
| Revisions | Selected domain revisions/generations exist; no universal HTTP contract | Existing mutation services | Mutable resources | ADR-0042, ADR-0048 | `If-Match` / expected revision inconsistent | Lost updates and stale writes | Slice 4 | Pending | Matrix | Shared envelope | Phase 67 API |
| Idempotency keys | Selected operation IDs/duplicate controls; no common header or durable replay contract | Existing action/workflow services | Remote and selected workflows | ADR-0042, ADR-0048 | `Idempotency-Key` not universal or conflict-checked | Retries can duplicate effects | Slice 4 | Pending | Matrix | Durable operations | Phase 64/65 |
| Operation IDs | Remote requires `operationId` and audits it | Remote domain and gate | Remote POST | ADR-0042, ADR-0049 | Other routes inconsistent; no common lifecycle store | Retry resolution incomplete | Slice 4 | operation-context tests and real-VDR operation `phase62-managed-1785212278` | Architecture | Operation repository | Phase 65 media sessions |
| Accountability event model | Append-only allow/deny rows with update/delete blockers | `AccountabilityEventRepository` | Current authentication failures and Remote authorization | ADR-0049 | Browser login/logout, cookie/CSRF events, outbox, retention, query/export and completion events missing | Incomplete forensic evidence | Slice 2 integration and Slice 5 | current append-only/gate/real-VDR evidence; no staged browser audit claim | Architecture | SQLite/request context | Phase 67 audit API |
| Audit actor and device | Verified runtime actors, including managed actor, are attributed; staged browser context contains actor/device/session but is not audited through Gate | gate; staged browser authenticator | Current Remote/access denials; future browser requests | ADR-0049 | Browser credential ID, issuance/logout/CSRF events and full auth lifecycle catalogue missing | Audit catalogue remains partial | Slice 2 integration then Slice 5 | current field/real-VDR rows; staged identity-context test | Architecture | Identity issuance | Phase 63 agents |
| Audit failure semantics | Required pre-dispatch append failure returns 503 | gate | Remote | ADR-0042, ADR-0049 | No transactional outbox joining domain result/completion; browser issuance/audit transaction not defined | Other mutations or future login could otherwise dispatch/issue without complete evidence | Slice 2 issuer design and Slice 5 | audit-unavailable test | Slice 1 | Durable persistence | None |
| Security error codes | Machine-readable nested errors with request ID and `no-store`; invalid enforced GET is not downgraded | gate | Current security failures | ADR-0048 | Cookie/CSRF/login/logout errors and other route shapes missing | Mixed client shapes | Slice 2 integration then Slice 3/4 convergence | current error/no-secret tests and real-VDR evidence | Architecture | HTTP response model | Phase 67 stable errors |
| Browser compatibility | Legacy Basic remains current browser path; managed Basic coexists; staged browser session verifier changes no request behavior | current configuration/authenticators/gate; staged browser repository/authenticator | Existing frontend; future login/session flow | ADR-0041 controlled migration | No issuer, secure cookie policy, Gate integration, CSRF enforcement, logout or migration switch | Shared legacy credential remains broadly privileged and public exposure unsafe | Remaining Slice 2 then Slice 6 | frontend/current gate/real-VDR coexistence plus staged verifier tests | Slice 2 | Browser session integration | Phase 67 release |
| Native client compatibility | Identity, verifier, request IDs, scoped grants, and errors are transport-neutral | security domain | Unversioned compatibility API | ADR-0048; proposed ADR-0051 not accepted | No native token/session API or stable `/api/v1` | Native clients cannot rely on final contract | Slice 2-5 preparation | Pending native issuance | Matrix | Session/token runtime | Phase 67 API/SDK |
| Administrative operations | Existing admin-like routes lack actor-specific permissions; no security-management API | router/controllers | Cache refresh, fuzzy operations, future identity lifecycle | ADR-0013, ADR-0049 | No `admin.*` catalogue or protected issuance/revoke/reactivate lifecycle management | High-impact operations remain under legacy broad credential | Slice 3, with issuer/logout prerequisite in Slice 2 | Pending | Matrix | Roles/permissions | None |
| Credential/token leak prevention | Basic submissions are not persisted/reflected; staged browser repository stores only token ID and one-way session/CSRF hashes | authenticators, verifier/lifecycle repositories, gate, audit | Current and future security paths | ADR-0041, ADR-0048, ADR-0049 | Repository-wide log redaction, response/cookie handling and frontend storage policy remain | Future issuer/integration code could leak raw secrets | Slice 2 guard plus hardening | wrong-password/no-secret tests, browser plaintext-negative test, architecture guards and real-VDR inspection | Architecture/Slice 2 | Logging and response inventory | Phase 67 hardening |

## Mutating and stateful POST inventory

| Route family | Runtime owner | Classification at Phase 62 start | Phase 62 migration target |
|---|---|---|---|
| `/api/vdr/remote/actions` | Remote runtime/controller/service | Mutating with operation ID, allowlist, capability, backend policy | Migrated in Slice 1 for current Basic contexts; future browser cookie path additionally requires CSRF before authorization/dispatch |
| Recording execute aliases | Recording action services | Mutating | Slice 3 permission and browser CSRF; Slice 4 revision/idempotency |
| Recording validate/preview aliases | Validation/preview | Non-mutating POST | Explicit safe/CSRF classification in Slice 3 |
| Timer create/update/delete | Timer services | Mutating | Slice 3 permission and browser CSRF; Slice 4 contracts; Phase 64 retains orchestration |
| Channel move aliases | Channel service | Mutating | Slice 3 permission and browser CSRF; Slice 4 contracts |
| SearchTimer create/update/delete/execute | SearchTimer services | Mixed mutation workflow | Slice 3 permission and browser CSRF; Slice 4 operation contract |
| SearchTimer validate/plan/preview | Planning services | Non-mutating POST | Explicit safe/CSRF classification in Slice 3 |
| EPG/cache/native-fuzzy administration | Admin controllers/services | Administrative state change | Slice 3 `admin.*` permission and browser CSRF |
| Future browser login/logout | Security runtime | Credential/session lifecycle | Slice 2 atomic issuance, secure cookie response, logout revocation and accountability |

## Phase 62 slice order

1. **Slice 1 Security identity foundation and first protected mutation — implemented and real-runtime validated**  
   Canonical context, central permission/scope decision, explicit legacy adapter, Remote enforcement, request/correlation IDs, and append-only pre-dispatch evidence.
2. **Slice 2 Persistent identities, credentials, devices, sessions and revocation — active**  
   Real-runtime accepted: persistence, request-time lifecycle resolution, managed identity provisioning, strict Basic verifier, secret-safe failure handling, legacy-bypass separation and backend-scoped Remote authorization. Implemented/tested foundation: persistent browser-session token binding, one-way session/CSRF hashes, strict cookie parser, expiry and revocation. Still required: atomic secure issuance, login/logout, `Set-Cookie` policy, authentication precedence, resolver/Gate integration, actual CSRF enforcement, native/service credentials, rotation/recovery, cleanup and protected lifecycle administration.
3. **Slice 3 Roles, permissions, scopes and complete route migration**  
   Permission catalogue, persistent roles/grants/scopes, administrative permissions, and explicit safe POST/CSRF classification.
4. **Slice 4 Common mutation envelope, revisions, idempotency and operation lifecycle**  
   Shared preconditions, `If-Match`, `Idempotency-Key`, replay/conflict semantics, and durable operations.
5. **Slice 5 Complete accountability catalogue, transactional outbox and protected queries**  
   Authentication/session/CSRF/mutation outcomes, outbox, protected audit reads, export, and retention.
6. **Slice 6 Compatibility retirement readiness and Phase 62 closeout**  
   Migration controls, negative end-to-end suite, documentation truth, and proof that Phase 63-67 runtime was not pulled forward.

Phase 62 remains open. The next strict work remains inside Slice 2: atomic browser-session issuance plus HTTP login/logout, secure cookie attributes, resolver/Gate integration, and real CSRF enforcement—not Android or later-phase runtime.
