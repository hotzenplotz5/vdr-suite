# Backend Registry

Goals:

- store configured backends
- keep backend metadata
- support manual entries
- prepare future discovery results

Expected Result:

Clients and services can use a stable registry of known backends.
