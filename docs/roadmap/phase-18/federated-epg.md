# Federated EPG

Goals:

- EPG data from multiple backends
- backend origin display
- backend scoped programme views
- avoid hidden data merging

Expected Result:

Clients can inspect EPG data across configured backends while preserving source context.
