# Federated Timers

Goals:

- timers from multiple backends
- backend ownership display
- backend scoped timer actions
- conflict aware timer views

Expected Result:

Clients can inspect and manage timers across configured backends safely.
