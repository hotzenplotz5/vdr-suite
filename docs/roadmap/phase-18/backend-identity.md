# Backend Identity

Goals:

- unique backend identity
- stable backend naming
- visible backend information
- support federation scenarios

Expected Result:

Clients can reliably distinguish multiple configured backends.
