# Offline Behaviour

Goals:

- visible offline state
- graceful backend loss handling
- no hidden write retries
- clear user feedback

Expected Result:

Clients remain predictable when one backend is unavailable.
