# Client Context

Goals:

- show selected backend
- keep client state visible
- separate local and remote data
- avoid accidental cross backend actions

Expected Result:

Clients make the active backend context clear to users.
