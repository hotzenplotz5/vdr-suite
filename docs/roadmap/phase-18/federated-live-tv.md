# Federated Live TV

Goals:

- live TV from multiple backends
- backend origin display
- backend scoped live views
- prepare future stream selection

Expected Result:

Clients can access live TV sources across configured backends while preserving backend context.
