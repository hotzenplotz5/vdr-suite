# Remote Actions

Goals:

- execute actions on remote backends
- preserve backend ownership
- expose action targets clearly
- support audit and permissions

Expected Result:

Clients can safely execute actions on remote backends with full context.
