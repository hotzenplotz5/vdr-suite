# Conflict Handling

Goals:

- detect conflicting backend states
- avoid silent overwrites
- require explicit user decisions
- document resolution rules

Expected Result:

Federation handles conflicting states predictably and transparently.
