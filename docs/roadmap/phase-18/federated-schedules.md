# Federated Schedules

Goals:

- schedules from multiple backends
- backend ownership display
- unified navigation
- clear active backend context

Expected Result:

Clients can inspect schedules across multiple backends.
