# Federated Streams

Goals:

- stream sources from multiple backends
- backend origin display
- stream capability awareness
- prepare future playback routing

Expected Result:

Clients can reason about stream sources across configured backends safely.
