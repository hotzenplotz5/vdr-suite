# Remote Search

Goals:

- search across multiple backends
- show backend origin for results
- support backend scoped searches
- avoid mixing ownership silently

Expected Result:

Clients can search federated data while preserving backend context.
