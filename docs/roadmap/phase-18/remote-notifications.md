# Remote Notifications

Goals:

- notify clients about remote backend events
- show backend origin
- support job and system events
- avoid duplicate notifications

Expected Result:

Clients can receive clear notifications from multiple backends.
