# Action Safety

Goals:

- execute actions on the selected backend only
- show target backend before write actions
- prevent accidental cross backend writes
- require clear user intent

Expected Result:

Clients avoid unsafe actions when multiple backends are configured.
