# Federated Channels

Goals:

- channels from multiple backends
- backend origin display
- backend scoped channel views
- avoid assuming shared channel lists

Expected Result:

Clients can inspect channel data across configured backends while preserving source context.
